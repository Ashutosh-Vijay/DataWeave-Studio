{
  "id": "invoice-lines-optional-quantity-predict-0",
  "unit": "default-and-missing",
  "mode": "predict",
  "tier": "easy",
  "title": "Without parentheses, on a line that has a quantity",
  "topics": [
    "default",
    "map",
    "precedence"
  ],
  "prompt": "What does this return? Read it — do not run it.",
  "inputMime": "application/json",
  "outputMime": "application/json",
  "given": {
    "script": "%dw 2.0\noutput application/json\n---\npayload.qty default 1 * payload.price",
    "payload": "{\"qty\":3,\"price\":5}"
  },
  "cases": [],
  "solution": "%dw 2.0\noutput application/json\n---\npayload.qty default 1 * payload.price",
  "explanation": {
    "approach": "The quantity comes back on its own. The multiplication only happens in the fallback.",
    "snippets": [
      {
        "label": "Without parentheses, on a line that has a quantity",
        "script": "%dw 2.0\noutput application/json\n---\npayload.qty default 1 * payload.price",
        "payload": "{\"qty\":3,\"price\":5}",
        "expect": {
          "output": "3"
        },
        "note": "The quantity comes back on its own. The multiplication only happens in the fallback."
      }
    ]
  },
  "derivedFrom": "invoice-lines-optional-quantity"
}
