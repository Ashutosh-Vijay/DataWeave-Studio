{
  "id": "split-customer-name-predict-1",
  "unit": "string-basics",
  "mode": "predict",
  "tier": "easy",
  "title": "Trim, then split on a regex",
  "topics": [
    "splitBy",
    "trim",
    "regex"
  ],
  "prompt": "What does this return? Read it — do not run it.",
  "inputMime": "application/json",
  "outputMime": "application/json",
  "given": {
    "script": "%dw 2.0\noutput application/json\n---\ntrim(\"  Alan   Turing \") splitBy /\\s+/"
  },
  "cases": [],
  "solution": "%dw 2.0\noutput application/json\n---\ntrim(\"  Alan   Turing \") splitBy /\\s+/",
  "explanation": {
    "approach": "One piece per word.",
    "snippets": [
      {
        "label": "Trim, then split on a regex",
        "script": "%dw 2.0\noutput application/json\n---\ntrim(\"  Alan   Turing \") splitBy /\\s+/",
        "expect": {
          "output": "[\"Alan\",\"Turing\"]"
        },
        "note": "One piece per word."
      }
    ]
  },
  "derivedFrom": "split-customer-name"
}
