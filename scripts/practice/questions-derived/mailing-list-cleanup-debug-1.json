{
  "id": "mailing-list-cleanup-debug-1",
  "unit": "string-basics",
  "mode": "debug",
  "tier": "easy",
  "title": "Fix it: clean up a mailing list typed in by hand",
  "topics": [
    "trim",
    "lower",
    "filter",
    "isEmpty"
  ],
  "basics": "`trim(s)` removes spaces from both ends of a String, and `lower(s)` turns it to lower case. They are ordinary functions, so they nest: `lower(trim(s))`. Cleaning text usually means applying a few of these in the right order.",
  "prompt": "The script in the editor is **almost** right. It passes on the sample and fails on a case you cannot see.\n\nThe original task was:\n\nSign-ups from a paper form were typed into a spreadsheet and arrive as an array of Strings. Before loading them into the mail platform:\n\n- remove leading and trailing spaces,\n- make every address lower case,\n- drop any entry that is blank.\n\nA blank entry may be an empty String or may be nothing but spaces. Keep the order of the rest.\n\nFind what is wrong and fix it. You can press Run to see what it currently does.",
  "inputMime": "application/json",
  "outputMime": "application/json",
  "starter": "%dw 2.0\noutput application/json\n---\npayload map lower($) filter not isEmpty(trim($))",
  "cases": [
    {
      "input": "[\n  \"Ann@Example.com\",\n  \"bo@example.com\",\n  \"\"\n]",
      "output": "[\"ann@example.com\",\"bo@example.com\"]"
    },
    {
      "input": "[\n  \" Ann@Example.com\",\n  \"bo@example.com \"\n]",
      "output": "[\"ann@example.com\",\"bo@example.com\"]"
    },
    {
      "input": "[\n  \"   \",\n  \"CY@EXAMPLE.COM\"\n]",
      "output": "[\"cy@example.com\"]"
    },
    {
      "input": "[\n  \"dee@example.com\"\n]",
      "output": "[\"dee@example.com\"]"
    },
    {
      "input": "[]",
      "output": "[]"
    }
  ],
  "hints": [
    "Run it on the sample first. If the sample looks right, the bug only shows on data the sample does not contain.",
    "Ask what kind of input would break this — an empty list, a missing field, a value ordered differently.",
    "lower-cases but never trims, so addresses keep their stray spaces"
  ],
  "solution": "%dw 2.0\noutput application/json\n---\npayload map lower(trim($)) filter not isEmpty($)",
  "mustFail": [
    {
      "why": "the unfixed script",
      "script": "%dw 2.0\noutput application/json\n---\npayload map lower($) filter not isEmpty(trim($))"
    }
  ],
  "explanation": {
    "approach": "Order matters. A String of spaces is not empty, so it passes an `isEmpty` check until it has been trimmed. Clean every entry first, then filter on the cleaned values.",
    "snippets": [
      {
        "label": "Spaces are not empty",
        "script": "%dw 2.0\noutput application/json\n---\n[isEmpty(\"   \"), isEmpty(trim(\"   \"))]",
        "expect": {
          "output": "[false,true]"
        },
        "note": "Only after trimming does it count as blank."
      },
      {
        "label": "Filtering first",
        "script": "%dw 2.0\noutput application/json\n---\npayload filter not isEmpty($) map lower(trim($))",
        "payload": "[\"   \",\"CY@EXAMPLE.COM\"]",
        "expect": {
          "output": "[\"\",\"cy@example.com\"]"
        },
        "note": "The spaces-only entry slipped through and came out as an empty String."
      }
    ]
  },
  "derivedFrom": "mailing-list-cleanup"
}
