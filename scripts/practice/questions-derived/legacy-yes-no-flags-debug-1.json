{
  "id": "legacy-yes-no-flags-debug-1",
  "unit": "type-coercion",
  "mode": "debug",
  "tier": "medium",
  "title": "Fix it: turn a legacy system's y/n and 1/0 flags into booleans",
  "topics": [
    "as",
    "Boolean",
    "contains",
    "upper"
  ],
  "basics": "`as Boolean` converts only the text \"true\" and \"false\". Other ways of writing yes and no, such as \"Y\" or \"1\", need your own rule. `contains` checks whether an Array holds a value: `[\"Y\", \"1\"] contains \"Y\"` is true.",
  "prompt": "The script in the editor is **almost** right. It passes on the sample and fails on a case you cannot see.\n\nThe original task was:\n\nA legacy system exports flags in whatever form each screen used: `\"Y\"`/`\"N\"`, `\"y\"`/`\"n\"`, `\"1\"`/`\"0\"`, the Numbers `1`/`0`, or `\"TRUE\"`/`\"false\"`.\n\nReturn each flag as a Boolean: `true` for Y, 1 and true in any case, `false` for everything else.\n\nFind what is wrong and fix it. You can press Run to see what it currently does.",
  "inputMime": "application/json",
  "outputMime": "application/json",
  "starter": "%dw 2.0\noutput application/json\n---\npayload map ([\"Y\", \"1\", \"TRUE\", \"true\", \"y\"] contains $)",
  "cases": [
    {
      "input": "[\n  \"Y\",\n  \"N\",\n  \"Y\"\n]",
      "output": "[true,false,true]"
    },
    {
      "input": "[\n  \"y\",\n  \"n\"\n]",
      "output": "[true,false]"
    },
    {
      "input": "[\n  1,\n  0,\n  \"1\"\n]",
      "output": "[true,false,true]"
    },
    {
      "input": "[\n  \"TRUE\",\n  \"false\",\n  \"True\"\n]",
      "output": "[true,false,true]"
    }
  ],
  "hints": [
    "Run it on the sample first. If the sample looks right, the bug only shows on data the sample does not contain.",
    "Ask what kind of input would break this — an empty list, a missing field, a value ordered differently.",
    "does not convert the value to text first, so the Number 1 is not found in a list of Strings"
  ],
  "solution": "%dw 2.0\noutput application/json\n---\npayload map ([\"Y\", \"1\", \"TRUE\"] contains upper($ as String))",
  "mustFail": [
    {
      "why": "the unfixed script",
      "script": "%dw 2.0\noutput application/json\n---\npayload map ([\"Y\", \"1\", \"TRUE\", \"true\", \"y\"] contains $)"
    }
  ],
  "explanation": {
    "approach": "A lookup of the values that mean yes is clearer than a chain of comparisons. The value has to be normalised first: converting to String makes the Number 1 and the text \"1\" the same, and upper-casing makes \"y\" and \"Y\" the same.",
    "snippets": [
      {
        "label": "Only true and false coerce",
        "script": "%dw 2.0\noutput application/json\n---\n\"Y\" as Boolean",
        "expect": {
          "error": true
        },
        "note": "Cannot coerce String (Y) to Boolean."
      },
      {
        "label": "A Number is not found among Strings",
        "script": "%dw 2.0\noutput application/json\n---\n[\"1\"] contains 1",
        "expect": {
          "output": "false"
        },
        "note": "`contains` compares type as well as value."
      }
    ]
  },
  "derivedFrom": "legacy-yes-no-flags"
}
