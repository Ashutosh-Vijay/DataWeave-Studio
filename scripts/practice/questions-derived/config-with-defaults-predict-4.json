{
  "id": "config-with-defaults-predict-4",
  "unit": "default-and-missing",
  "mode": "predict",
  "tier": "easy",
  "title": "zero is NOT null",
  "topics": [
    "default",
    "null"
  ],
  "prompt": "What does this return? Read it — do not run it.",
  "inputMime": "application/json",
  "outputMime": "application/json",
  "given": {
    "script": "%dw 2.0\noutput application/json\n---\n0 default 3"
  },
  "cases": [],
  "solution": "%dw 2.0\noutput application/json\n---\n0 default 3",
  "explanation": {
    "approach": "Zero is a number value, not null. It passes through.",
    "snippets": [
      {
        "label": "zero is NOT null",
        "script": "%dw 2.0\noutput application/json\n---\n0 default 3",
        "expect": {
          "output": "0"
        },
        "note": "Zero is a number value, not null. It passes through."
      }
    ]
  },
  "derivedFrom": "config-with-defaults"
}
