{
  "id": "parse-settings-form-predict-5",
  "unit": "type-coercion",
  "mode": "predict",
  "tier": "easy",
  "title": "Full example with both conversions",
  "topics": [
    "as",
    "Number",
    "Boolean"
  ],
  "prompt": "What does this return? Read it — do not run it.",
  "inputMime": "application/json",
  "outputMime": "application/json",
  "given": {
    "script": "%dw 2.0\noutput application/json\n---\ndo {\n  var item = {name: \"item1\", count: \"10\", enabled: \"true\"}\n  ---\n  {\n    name: item.name,\n    count: item.count as Number,\n    enabled: item.enabled as Boolean\n  }\n}"
  },
  "cases": [],
  "solution": "%dw 2.0\noutput application/json\n---\ndo {\n  var item = {name: \"item1\", count: \"10\", enabled: \"true\"}\n  ---\n  {\n    name: item.name,\n    count: item.count as Number,\n    enabled: item.enabled as Boolean\n  }\n}",
  "explanation": {
    "approach": "This is the real-world pattern: receive string data, convert the fields that need to be numbers or booleans, and keep strings as strings where they belong.",
    "snippets": [
      {
        "label": "Full example with both conversions",
        "script": "%dw 2.0\noutput application/json\n---\ndo {\n  var item = {name: \"item1\", count: \"10\", enabled: \"true\"}\n  ---\n  {\n    name: item.name,\n    count: item.count as Number,\n    enabled: item.enabled as Boolean\n  }\n}",
        "expect": {
          "output": "{\n  \"name\": \"item1\",\n  \"count\": 10,\n  \"enabled\": true\n}"
        },
        "note": "This is the real-world pattern: receive string data, convert the fields that need to be numbers or booleans, and keep strings as strings where they belong."
      }
    ]
  },
  "derivedFrom": "parse-settings-form"
}
