{
  "id": "redact-secret-fields-predict-0",
  "unit": "tree-module",
  "mode": "predict",
  "tier": "medium",
  "title": "What a path looks like",
  "topics": [
    "dw::util::Tree",
    "mapLeafValues",
    "Path"
  ],
  "prompt": "What does this return? Read it — do not run it.",
  "inputMime": "application/json",
  "outputMime": "application/json",
  "given": {
    "script": "%dw 2.0\noutput application/json\nimport * from dw::util::Tree\n---\npayload mapLeafValues ((v, p) -> p.selector)",
    "payload": "{\"db\":{\"password\":\"p1\"}}"
  },
  "cases": [],
  "solution": "%dw 2.0\noutput application/json\nimport * from dw::util::Tree\n---\npayload mapLeafValues ((v, p) -> p.selector)",
  "explanation": {
    "approach": "One step per level. The last one is the field name.",
    "snippets": [
      {
        "label": "What a path looks like",
        "script": "%dw 2.0\noutput application/json\nimport * from dw::util::Tree\n---\npayload mapLeafValues ((v, p) -> p.selector)",
        "payload": "{\"db\":{\"password\":\"p1\"}}",
        "expect": {
          "output": "{\"db\":{\"password\":[\"db\",\"password\"]}}"
        },
        "note": "One step per level. The last one is the field name."
      }
    ]
  },
  "derivedFrom": "redact-secret-fields"
}
