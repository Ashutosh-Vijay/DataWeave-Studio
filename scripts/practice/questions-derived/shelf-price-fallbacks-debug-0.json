{
  "id": "shelf-price-fallbacks-debug-0",
  "unit": "default-and-missing",
  "mode": "debug",
  "tier": "medium",
  "title": "Fix it: pick the shelf price from a sale price, a list price, or neither",
  "topics": [
    "default",
    "if"
  ],
  "basics": "`default` can be chained: `a default b default c` gives the first of the three that is not null. It only ever replaces null, so a real value such as `0` or `false` is kept.",
  "prompt": "The script in the editor is **almost** right. It passes on the sample and fails on a case you cannot see.\n\nThe original task was:\n\nThe product feed has `{ sku, salePrice, listPrice }`, and either price may be missing. The shelf price is the sale price if there is one, otherwise the list price, otherwise 0.\n\nPromotional items are sometimes given away with a sale price of `0`. That is a real sale price and must be used.\n\nReturn `{ sku, price }` for each product.\n\nFind what is wrong and fix it. You can press Run to see what it currently does.",
  "inputMime": "application/json",
  "outputMime": "application/json",
  "starter": "%dw 2.0\noutput application/json\n---\npayload map { sku: $.sku, price: if (($.salePrice default 0) > 0) $.salePrice else ($.listPrice default 0) }",
  "cases": [
    {
      "input": "[\n  {\n    \"sku\": \"A\",\n    \"listPrice\": 10\n  },\n  {\n    \"sku\": \"B\",\n    \"salePrice\": 5\n  }\n]",
      "output": "[{\"sku\":\"A\",\"price\":10},{\"sku\":\"B\",\"price\":5}]"
    },
    {
      "input": "[\n  {\n    \"sku\": \"C\",\n    \"salePrice\": 7,\n    \"listPrice\": 9\n  }\n]",
      "output": "[{\"sku\":\"C\",\"price\":7}]"
    },
    {
      "input": "[\n  {\n    \"sku\": \"D\",\n    \"salePrice\": 0,\n    \"listPrice\": 12\n  }\n]",
      "output": "[{\"sku\":\"D\",\"price\":0}]"
    },
    {
      "input": "[\n  {\n    \"sku\": \"E\"\n  }\n]",
      "output": "[{\"sku\":\"E\",\"price\":0}]"
    }
  ],
  "hints": [
    "Run it on the sample first. If the sample looks right, the bug only shows on data the sample does not contain.",
    "Ask what kind of input would break this — an empty list, a missing field, a value ordered differently.",
    "tests the sale price with > 0, so a free promotional item gets its list price"
  ],
  "solution": "%dw 2.0\noutput application/json\n---\npayload map { sku: $.sku, price: $.salePrice default $.listPrice default 0 }",
  "mustFail": [
    {
      "why": "the unfixed script",
      "script": "%dw 2.0\noutput application/json\n---\npayload map { sku: $.sku, price: if (($.salePrice default 0) > 0) $.salePrice else ($.listPrice default 0) }"
    }
  ],
  "explanation": {
    "approach": "`default` answers \"is it missing?\", which is exactly the rule. A numeric test such as `> 0` answers a different question and loses the free items. The order of the chain is the order of preference.",
    "snippets": [
      {
        "label": "Zero is not missing",
        "script": "%dw 2.0\noutput application/json\n---\n0 default 99",
        "expect": {
          "output": "0"
        },
        "note": "`default` keeps any value that is not null."
      },
      {
        "label": "A chain",
        "script": "%dw 2.0\noutput application/json\n---\npayload.salePrice default payload.listPrice default 0",
        "payload": "{}",
        "expect": {
          "output": "0"
        },
        "note": "Both missing, so the last fallback is used."
      }
    ]
  },
  "derivedFrom": "shelf-price-fallbacks"
}
