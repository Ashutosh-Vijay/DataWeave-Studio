{
  "id": "phone-numbers-digits-only",
  "unit": "string-basics",
  "tier": "medium",
  "title": "Strip phone numbers down to digits for the SMS gateway",
  "topics": [
    "replace",
    "regex"
  ],
  "examObjective": "Applying correct syntax to write DataWeave transformations to coerce, format, order, and filter data",
  "basics": "`s replace x with y` replaces every match of x in s. The x can be a regular expression between slashes, where a character class such as `[^0-9]` means \"any character that is not a digit\".",
  "mode": "build",
  "prompt": "The SMS gateway accepts only digits, with an optional leading `+` for the country code. Customers typed their numbers every way imaginable: spaces, dashes, dots, brackets.\n\nReturn each number with everything except digits and `+` removed.",
  "inputMime": "application/json",
  "outputMime": "application/json",
  "starter": "%dw 2.0\noutput application/json\n---\n",
  "cases": [
    {
      "input": "[\n  \"020 7946 0000\",\n  \"0161 496 0000\"\n]",
      "output": "[\"02079460000\",\"01614960000\"]"
    },
    {
      "input": "[\n  \"+44 20 7946-0000\"\n]",
      "output": "[\"+442079460000\"]"
    },
    {
      "input": "[\n  \"(020) 7946.0000\",\n  \"+1-555-0100\"\n]",
      "output": "[\"02079460000\",\"+15550100\"]"
    },
    {
      "input": "[]",
      "output": "[]"
    }
  ],
  "hints": [
    "Replacing spaces alone passes the sample. Look at the other characters in the prompt.",
    "Remove every character that is not a digit or a plus. A regex character class can say \"not these\".",
    "`$ replace /[^0-9+]/ with \"\"`"
  ],
  "solution": "%dw 2.0\noutput application/json\n---\npayload map ($ replace /[^0-9+]/ with \"\")",
  "mustFail": [
    {
      "why": "removes only spaces, so dashes, dots and brackets stay in",
      "script": "%dw 2.0\noutput application/json\n---\npayload map ($ replace \" \" with \"\")"
    },
    {
      "why": "keeps digits only, which also strips the + of the country code",
      "script": "%dw 2.0\noutput application/json\n---\npayload map ($ replace /[^0-9]/ with \"\")"
    }
  ],
  "explanation": {
    "approach": "Listing every separator to remove never ends. Describe what to keep instead, and remove everything else with a negated character class.",
    "snippets": [
      {
        "label": "Everything but digits and +",
        "script": "%dw 2.0\noutput application/json\n---\n\"+44 (0)20 7946-0000\" replace /[^0-9+]/ with \"\"",
        "expect": {
          "output": "\"+4402079460000\""
        },
        "note": "The (0) is kept as a digit. Whether to drop it is a business rule, not a formatting one."
      },
      {
        "label": "Only the spaces",
        "script": "%dw 2.0\noutput application/json\n---\n\"+1-555-0100\" replace \" \" with \"\"",
        "expect": {
          "output": "\"+1-555-0100\""
        },
        "note": "The dashes are untouched."
      }
    ]
  }
}
