{
  "id": "count-comments-in-thread",
  "unit": "recursion",
  "tier": "hard",
  "title": "Count every comment in a discussion thread",
  "topics": [
    "fun",
    "recursion",
    "sum",
    "default"
  ],
  "examObjective": "Defining, using, and reusing DataWeave modules, functions, and variables",
  "basics": "A recursive function calls itself on a smaller part of the input. For nested data, the function handles one level and calls itself for each child; the recursion stops at the elements that have no children.",
  "mode": "build",
  "prompt": "The forum API returns a thread as its first comment, where every comment is `{ id, replies }` and `replies` is an array of comments in the same shape. A comment with no replies may have no `replies` field at all.\n\nReturn the total number of comments in the thread, including the first one.",
  "inputMime": "application/json",
  "outputMime": "application/json",
  "starter": "%dw 2.0\noutput application/json\n---\n",
  "cases": [
    {
      "input": "{\n  \"id\": 1,\n  \"replies\": [\n    {\n      \"id\": 2\n    },\n    {\n      \"id\": 3\n    }\n  ]\n}",
      "output": "3"
    },
    {
      "input": "{\n  \"id\": 1,\n  \"replies\": [\n    {\n      \"id\": 2,\n      \"replies\": [\n        {\n          \"id\": 4\n        },\n        {\n          \"id\": 5,\n          \"replies\": [\n            {\n              \"id\": 6\n            }\n          ]\n        }\n      ]\n    },\n    {\n      \"id\": 3\n    }\n  ]\n}",
      "output": "6"
    },
    {
      "input": "{\n  \"id\": 1\n}",
      "output": "1"
    },
    {
      "input": "{\n  \"id\": 1,\n  \"replies\": []\n}",
      "output": "1"
    }
  ],
  "hints": [
    "Counting the first comment and its direct replies works on the sample. The hidden threads go deeper.",
    "A comment counts as 1 plus the count of each of its replies, and each reply is counted the same way.",
    "`fun count(c) = 1 + sum((c.replies default []) map count($))`"
  ],
  "solution": "%dw 2.0\noutput application/json\nfun count(c) = 1 + sum((c.replies default []) map count($))\n---\ncount(payload)",
  "mustFail": [
    {
      "why": "counts only the first comment and its direct replies, which misses replies to replies",
      "script": "%dw 2.0\noutput application/json\n---\n1 + sizeOf(payload.replies default [])"
    },
    {
      "why": "recurses but forgets to count each comment itself, so it returns 0",
      "script": "%dw 2.0\noutput application/json\nfun count(c) = sum((c.replies default []) map count($))\n---\ncount(payload)"
    }
  ],
  "explanation": {
    "approach": "A thread has no fixed depth, so a fixed number of selectors cannot count it. A function that counts one comment and asks itself to count each reply handles any depth. The `default []` makes a comment without replies the place where the recursion stops, and `sum` of an empty Array is 0.",
    "snippets": [
      {
        "label": "The leaf case",
        "script": "%dw 2.0\noutput application/json\nfun count(c) = 1 + sum((c.replies default []) map count($))\n---\ncount({ id: 9 })",
        "expect": {
          "output": "1"
        },
        "note": "No replies: 1 plus the sum of nothing."
      },
      {
        "label": "One level only",
        "script": "%dw 2.0\noutput application/json\n---\n1 + sizeOf(payload.replies default [])",
        "payload": "{\"id\":1,\"replies\":[{\"id\":2,\"replies\":[{\"id\":4}]}]}",
        "expect": {
          "output": "2"
        },
        "note": "Comment 4 is never seen."
      }
    ]
  }
}
