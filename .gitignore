# Dependencies
node_modules

# Vendored cookbook source (sparse clone; only generated src/cookbookRecipes.ts is committed)
.cookbook-src

# Build output
dist
dist-ssr

# Microsoft Store / MSIX build artifacts (Package.appxmanifest + Assets/ ARE committed)
dist-msix/
*.msix
*.msixbundle
*.pfx

# Tauri build output
src-tauri/target

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment / secrets
*.local
.env
.env.*

# Editor directories and files
.vscode
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS files
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# Rust
*.pdb

# DW Studio temp files
dw-studio-tmp/

# Built dwstudio-server.jar lives here in dev (CI rebuilds it from dw-server/)
src-tauri/resources/dw-server/*.jar

# Bundled JRE (generated via jlink — too large for git)
src-tauri/resources/jre/
dw-server/target/

# Build artifacts / cached files
latest_release.json
AGENTS.md

# Local design handoff reference (unpacked from Claude Design bundle)
.design-spec/

# Claude Code agent state (worktrees, session metadata)
.claude/

# Playwright MCP artifacts (screenshots / console logs from live-app inspection)
.playwright-mcp/

# Graphify knowledge-graph output (generated)
graphify-out/

# Local sample data and AI-generated notes
example/
project_summary.md
FLOW_OVERHAUL_PLAN.md

# Development certificate
devcert.pfx
